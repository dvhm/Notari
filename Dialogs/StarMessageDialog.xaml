<Window x:Class="Notari.Dialogs.StarMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
        Title="Hey!"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="{StaticResource Brush.Surface}"
        Foreground="{StaticResource Brush.TextPrimary}"
        FontFamily="{StaticResource Font.Primary}"
        FontSize="{StaticResource FontSize.Base}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Divider}"
                BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Hey!"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource Brush.TextSecondary}"
                           FontSize="{StaticResource FontSize.Small}"
                           IsHitTestVisible="False"/>
                <Button Style="{StaticResource Style.CaptionButton.Close}"
                        HorizontalAlignment="Right"
                        Click="OnOk">
                    <ui:SymbolIcon Symbol="Dismiss" FontSize="11"/>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="24">

            <!-- Icon + message -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <ui:SymbolIcon Symbol="StarEmphasis"
                               Foreground="{StaticResource Brush.Accent}"
                               FontSize="28"
                               VerticalAlignment="Center"
                               Margin="0,2,14,0"/>
                <StackPanel>
                    <TextBlock Text="If you end up liking Notari, a star on"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="GitHub helps a lot :) Enjoy using it!"
                               Foreground="{StaticResource Brush.TextSecondary}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="BtnOk"
                        Content="Get started"
                        Width="110"
                        Style="{StaticResource Style.Button.Accent}"
                        Click="OnOk"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>